# プロジェクト構成と前提ルール (Project Structure & Rules)

このドキュメントは、「町屋やきにく密陽家（みらんちぷ）」専用の発注管理・予約計算アプリ（Flutter/Dart）の全体構造と、開発における**絶対厳守のルール**をまとめたものです。AI（Gemini）や新規開発者がプロジェクトを触る際の共通認識として機能します。

## ⚠️ 【重要】開発における絶対ルール (Core Principles)
1. **店舗名**: 「町屋やきにく密陽家」は**「みらんちぷ」**と読む。
2. **Firebaseデータ最優先（上書き厳禁）**:
   すでに現場（店舗）での運用が始まっており、発注品目や料理マスタなどのデータは現場スタッフの手によって大量に追加・更新されている。**コード側の初期データ（モックデータ）でFirebase上の本番データを絶対に上書き・初期化してはならない。** マスタ操作のロジックを変更する際は、既存データを破壊しないことを最優先とする。
3. **Toreta連携の現状**:
   現状、Toreta APIの取得は未実装（`reservation_data.dart` は空リストを返している）。そのため、予約からの必要食材自動計算ロジックは存在するが、API連携が完了するまでは実質的に機能していない状態である。

## 🎯 アプリケーション概要
* **Frontend**: Flutter (Dart)
* **Backend**: Firebase (Firestore)
* **主な機能**: 
  * リアルタイムな発注状況の共有（`snapshots()`利用）
  * 営業日管理（朝6時切り替え）と日別履歴保存
  * アルバイト（キッチン/裏方）向け・チーフ（仕入先別）向けのUI出し分け

## 📁 ディレクトリ構造と主要ファイル (Directory Tree)

* `.dart_tool/`, `.firebase/`, `android/`, `ios/`, `web/`, `macos/`, `windows/`: ビルド・設定用
* `firebase.json`, `.firebaserc`: Firebaseデプロイ・設定用
* `pubspec.yaml`: パッケージ依存関係
* **`lib/`**: メインソースコード

### 📦 lib 直下
* `main.dart`: エントリーポイント。Firebase初期化、マスタデータ取得、トップ画面描画。
* `firebase_options.dart`: Firebase初期化設定オプション。

### 📂 lib/data (データ処理・Firestore連携)
* `course_data.dart`: コースマスタの取得。
* `dish_data.dart`: 料理マスタの取得。サンチュ（sanchu_2p）や盛岡冷麺（reimen_step）、クッパ等の特例ルールが定義されている。
* `item_data.dart`: 発注品マスタの取得。
* `reservation_data.dart`: 予約データのフェッチ。（※現在はAPI未連携のため空リストを返す仕様）

### 📂 lib/models (データモデル定義)
* `course_recipe.dart`: `CourseRecipe`（コース構成）。
* `dish.dart`: `Dish`（料理）および `DishItemRequirement`（必要食材、歩留まり、テーブル固定フラグ等）。
* `item.dart`: `Item`（名前、厨房/裏方カテゴリ、最低在庫数など）。
* `order_item.dart`: `OrderItem`（実際の発注操作用モデル）。
* `reservation.dart`: `Reservation`（お客様予約情報）。

### 📂 lib/pages (UI・画面定義)
**【発注・日次業務】**
* `order_home_page.dart`: メイン画面。キッチン側/裏側の表示切り替え、日曜日特有の警告バナー、朝6時基準の営業日更新ロジック、および予約数に基づく必要量計算ロジックを内包する。
* `board_page.dart`: ホワイトボード画面（パート・アルバイト用）。
* `chief_page.dart`: チーフ画面（仕入先グループ別表示）。
* `previous_order_page.dart`: 過去の発注履歴閲覧。
* `memo_page.dart`: SharedPreferencesを用いたローカルメモ。

**【マスタ管理（※既存データ上書き注意）】**
* `course_edit_page.dart`: コースレシピマスタの編集。
* `dish_edit_page.dart`: 料理マスタの編集（必要食材の紐付け）。
* `master_edit_page.dart`: 発注品マスタの編集。
* `reservation_page.dart`: 予約一覧・伝票コピー用画面。




### ホールアイテム マスタデータ定義用リスト

1. タブ①＆②用（ドリンク・在庫カウント系） 
在庫管理（開栓・未開栓・ストック）および発注数の自動計算に使用するアイテム群です。

* 
**冷蔵庫①-上段**: ジンジャエール、コーラ、生マッコリ(開)、JINROマッコリ(開)、黒豆マッコリ(開) 


* 
**冷蔵庫①-下段**: アサヒスーパードライ、エビス、オールフリー、独歩ピルスナー、マスカットピルス 


* 
**冷蔵庫②-上段**: りんごスパークリング、レモネード 


* 
**冷蔵庫②-下段**: ウーロン茶、黒ウーロン茶、オレンジ、久保田(開)、奇跡のお酒(開)、サンタヘレナ(開)、ピーチシロップ、ライムシロップ、白ぶどうシロップ、巨峰シロップ 


* 
**冷蔵庫③-1段目**: ジンジャエール、コーラ、オールフリー 


* 
**冷蔵庫③-2段目**: ウーロン茶、黒ウーロン茶、オレンジ 


* 
**冷蔵庫③-3段目**: アサヒスーパードライ、生マッコリ、JINROマッコリ、黒豆マッコリ、ジネステ、アルベール、ドゥルト 


* 
**冷蔵庫③-4段目**: 久保田、奇跡のお酒、ボルサオ、ファウドアランチョ、サンタヘレナ、リブランディ・チロ・ロッ、ガロ・カーニヴォ、カロン・セギュール、フレシネX、フェリスタス、カステル・ウエトロ、モエ・ブリュット、モエ・アイス、ドンペリ 


* 
**ジョッキ冷蔵庫**: ゆずジャム、いちじくシロップ、いちごシロップ、洋ナシシロップ、巨峰シロップ、カルピス 


* 
**ビール等**: ビール樽、ガス 


* 
**バー等に報告**: 抹茶アイス(キッチン)、バニラアイス(キッチン)、吉備団子、大手まんじゅう、白玉、汎用あんこ、ホイップクリーム、ほうじ茶ラテ粉 


* 
**棚**: レモンサワー、知多ハイボール、白州ハイボール、ジムビーム、カシスリキュール、すっきり梅酒、黒霧島、二階堂、香春梅、喜平、ハチミツしょうが、中々、白霧島、桜島あらわざ、㐂六、ウイスキー(5ℓ)、宝焼酎(酎ハイ用)、箸、箸袋、付けダレ、ワイン 


* *※「ワイン」には実装時に `memo: "必要数になるまで発注しないこと"` のような属性を付与します。*



2. タブ③用（補充・消耗品系） 
チェックボックスで発注有無のみを管理するアイテム群です。

* 
**毎日チェック**: ガム、アメ、ストロー、コチュジャン、にんにく、付けダレ、塩ポン酢、ポンズ、冷麺用お酢、箸セット、箸セット(テイクアウト)、おしぼり、シロップ・焼酎類、ライズ(トイレ)、紙コップ(トイレ)、綿棒(トイレ)、爪楊枝(トイレ)、歯ブラシ(トイレ)、ナプキン(トイレ)、ガム(トイレ) 


* 
**日曜日チェック**: ラムネ、紙ナプキン、ブラックペッパー、岩塩、子ども用タレ、アルコール(掃除用)、アルコール(客席・裏)、マイペット(薄める)、ハンドソープ(薄める)、リセッシュ、爪楊枝、コーヒーフレッシュ、コーヒーシュガー、ホットウーロン 

